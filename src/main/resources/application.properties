spring.application.name=app
server.port=8080 
# Definicion del Endpoint principal de la aplicacion
# server.servlet.context-path=/betplay/api
# Conexion a la base de datos en PostgreSQL
    spring.datasource.url=jdbc:postgresql://monorail.proxy.rlwy.net:33640/betplay
    spring.datasource.username=postgres
    spring.datasource.password=uBNnyIuFIdTwLZJLJeWcLYqiSvJBcLSn
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect
    spring.jpa.properties.hibernate.jdbc.time_zone= UTC
# Conexion a MYSQL
    # spring.datasource.url=jdbc:mysql://localhost:8080/betplay?createDatabaseIfNotExist=true 
    # spring.datasource.username=root
    # spring.datasource.password==5511
    # spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
    # spring.jpa.database-platform=org.hibernate.dialectMySQLDialect
    # spring.jpa.hibernate.ddl-auto=update
    # spring.jpa.properties.hibernate.jdbc.time_zone= UTC
    # spring.jpa.show-sql=true